# In-App SDK

Package:

```text
packages/gns-in-app
```

Exports:

- `GnsProvider`
- `useGnsNotifications`
- `useGnsUnreadCount`
- `useGnsNotificationStream`
- `useGnsNotificationPreferences`
- `GnsToastContainer`
- `GnsNotificationBell`
- `GnsNotificationCenter`
- `GnsNotificationItem`

The SDK uses `fetch` streaming so bearer tokens stay in request headers.

